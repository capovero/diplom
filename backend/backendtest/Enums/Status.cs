namespace backendtest.Models;

public enum Status // - `Status` (enum) — статус проекта (Pending, Approved, Rejected, Active, Completed).
{
    Pending,
    Approved,
    Rejected,
    Active,
    Completed
}
